import styled from "styled-components";

const NavBarLeftSide = () => {
  return (
    <NavBarLeftSideStyled className="left-side">Left</NavBarLeftSideStyled>
  );
};

const NavBarLeftSideStyled = styled.div`
  background: pink;
`;

export default NavBarLeftSide;
